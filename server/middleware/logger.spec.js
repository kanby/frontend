import { expect } from 'chai';

describe('server:middleware/logger', () => {
  it('logs request method', () => {
    expect.fail();
  });

  it('logs request path', () => {
    expect.fail();
  });
});
